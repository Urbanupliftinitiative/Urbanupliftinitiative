// Real photos, not stock/generated images — reusing the same
// program → photo mapping ProgramsSection.js already uses, so the Senior
// Safety Initiative reads as the same visual story everywhere it appears.
import FIRETRUCK_VOLUNTEER from '../../assets/gallery/firetruck-volunteer.jpg';

const eventImages = {
  'co-alarm-distribution': {
    src: FIRETRUCK_VOLUNTEER,
    alt: 'A UUI volunteer with the Camden Fire Department, geared up for community outreach',
  },
};

export default eventImages;
